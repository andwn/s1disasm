# ---------------------------------------------------------------------------
# Sprite mappings - invisible lava tag (MZ)
# ---------------------------------------------------------------------------
Map_LTag:	dc.w AT_84_0-Map_LTag
AT_84_0:		dc.b 0		/* no sprite, because the tag is invisible! */
		.align 2
